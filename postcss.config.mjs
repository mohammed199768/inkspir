// © Mohammad Aldomi — Project Source Code

/**
 * PostCSS configuration.
 * We use Tailwind CSS for styling and Autoprefixer to ensure cross-browser compatibility.
 * @type {import('postcss-load-config').Config}
 */
const config = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
};

export default config;
